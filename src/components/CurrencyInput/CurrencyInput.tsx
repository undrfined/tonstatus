import React, { FC } from "react";
import styles from "./CurrencyInput.module.scss";

const CurrencyInput: FC = () => {
  return <div className={styles.root}>wow!</div>;
};

export default CurrencyInput;
